import { ASSCSS } from "./ASCSS";
import { ASNode } from "./ASNode";
import { BookNode } from "./BookNode";
import { SheetNode } from "./SheetNode";

ASSCSS.SectionNode = {
};

export class SectionNode extends SheetNode {

}

customElements.define("section-node", SectionNode);
